# F71 — preflight-truthfulness paired-test baseline (ADR-024 Bundle C).
#
# This file lists check_<name> entries (encoded as
# kairix/core/db/integrity.py::<function_name>) that are grandfathered
# without a paired truthfulness contract test in
# tests/contracts/test_integrity_truthfulness.py.
#
# Paid down to ZERO at landing (2026-05-28) — every preflight that
# reports a SQL-derived count has a paired test; the one preflight that
# can't (vector-store-vs-content-vectors counts external usearch index
# length, not a SQL aggregate) is marked
# # F71-truthfulness-exempt: ... in integrity.py and skipped by the gate.
#
# Adding new entries here is rare. The mechanical motivation lives in
# scripts/checks/check_f71_preflight_truthfulness.py and the rationale
# lives in docs/architecture/ADR-024-test-pyramid-redesign.md §F71.
