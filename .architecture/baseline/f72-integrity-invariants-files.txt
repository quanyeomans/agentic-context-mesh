# F72 — cross-layer integrity invariants paired-test baseline (ADR-024 Bundle E).
#
# Per-invariant entries are encoded as
# tests/integrity_invariants/test_<name>.py::<missing_requirement>
# where <missing_requirement> is one of:
#   * missing_file
#   * missing_fixture_test
#   * missing_soak_test
#   * missing_invariant_marker_on_fixture_test
#   * missing_invariant_marker_on_soak_test
#   * missing_soak_marker_on_soak_test
#   * unreadable_file
#   * unparseable_file
#
# Paid down to ZERO at landing (2026-05-28) — all five seed invariants
# in scripts/checks/_integrity_invariants_registry.py ship with their
# matching test files containing both fixture and soak variants:
#   * bronze_coverage_parity
#   * content_vectors_alignment
#   * staging_drain_progress
#   * documents_media_extractor_completeness
#   * cc_pair_lifecycle_consistency
#
# Adding new entries here is rare. The mechanical motivation lives in
# scripts/checks/check_f72_integrity_invariants.py and the rationale
# lives in docs/architecture/ADR-024-test-pyramid-redesign.md §F72.
