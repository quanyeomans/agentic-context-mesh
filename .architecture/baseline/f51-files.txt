# F51 baseline — feature flags past target_retire_in without a retire-extension
# rationale comment.
#
# Empty at landing — kairix/core/features/ has not yet declared any flags
# (PR-2 lands the registry as empty; first flag arrives in IM-6 / Wave 5).
# F51 stays vacuous-green until the registry contains entries with
# deadlines that have already passed.
#
# Forward-only: net-new entries are blocked at pre-commit + CI Stage 0.
# To pay down: either retire the flag (delete the REGISTRY entry + remove
# the legacy code path) OR add a '# retire-extension: <reason>' comment
# adjacent to the entry in kairix/core/features/registry.py.
#
# See docs/architecture/feature-flag-architecture.md §4.1 + §6.
