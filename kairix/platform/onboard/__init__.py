"""
kairix.platform.onboard — deployment diagnostics and agent onboarding.

Provides:
  check.py    — deployment health checks (PATH, secrets, vector search, Neo4j)
  cli.py      — `kairix onboard` subcommand (check, guide, verify)
"""
